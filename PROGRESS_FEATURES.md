# 后羿之弓功能说明

> 本文按功能模块说明当前 1.0.5 的行为、实现边界、兼容关系和验证方法。
>
> - 按版本开发路程：[PROGRESS.md](PROGRESS.md)
> - 构建与 Windows 操作记录：[PROGRESS_OPERATIONS.md](PROGRESS_OPERATIONS.md)
> - 崩溃与失败尝试：[BugGress.md](BugGress.md)

## 1. 运行环境

| 项目 | 要求 |
| --- | --- |
| Minecraft | 1.21.1 |
| Mod Loader | NeoForge 21.1.233 或兼容 21.1.x |
| Java | 21 |
| 当前 Mod 版本 | 1.0.5 |
| JEI | 可选，集成目标版本 19.39.0.372 |
| 客户端与服务端 | 建议安装相同后羿之弓版本 |

不支持 Forge、Fabric 或其它 Minecraft 大版本。通过 Connector 加载的其它 Mod 不属于本项目修改范围。

## 2. 独立物品与原版弓边界

### 2.1 后羿之弓

- 注册 ID：`houyis_bow:houyis_bow`。
- 是独立物品，不是普通弓的全局数值覆盖。
- 默认耐久：1000。
- 使用原版弓的使用动作、持有姿势、弹药选择和拉弓动画属性。

### 2.2 普通原版弓

- 耐久保持原版。
- 拉弓时间保持原版。
- 箭速保持原版。
- 单发箭行为保持原版。
- 弹道和随机误差保持原版。
- 后羿弓的多箭、音波、拖尾、高速同步和自带击退不会应用到普通弓。

### 2.3 创造模式显示

- 后羿之弓加入原版“战斗”标签页。
- 同时进入创造模式搜索聚合结果。
- 没有创建单独的后羿之弓标签页。
- 该功能从 1.0.5 开始提供。

## 3. 材质与拉弓动画

- 物品材质尺寸：64×64。
- 共四个状态：空闲、拉弓第一段、拉弓第二段、拉满第三段。
- 模型沿用用户确认的弓身结构。
- 拉弓动画复用原版弓的 `pull` 和 `pulling` 物品属性。
- 箭头使用用户指定的金色体系，箭尾使用深青绿色体系。
- 客户端按后羿弓自己的满蓄力 tick 数计算拉弓帧，不使用固定原版 20 tick 比例。

验证方法：手持后羿之弓持续右键，确认空闲和三段拉弓材质按蓄力进度切换；快速装填高等级时仍应在缩短后的总时间内完成动画。

## 4. 获取与锻造配方

### 4.1 材料

| 槽位 | 材料 | 数量 |
| --- | --- | --- |
| 模板槽 | 下界合金升级锻造模板 | 3 |
| 基础槽 | 普通弓 | 1 |
| 添加槽 | 下界合金锭 | 12 |
| 输出 | 后羿之弓 | 1 |

### 4.2 合成行为

- 使用原版锻造台界面。
- 材料不足时不产生结果。
- 取出成品时总计扣除 3 个模板、1 把普通弓和 12 个下界合金锭。
- 成品通过原弓 `transmuteCopy` 生成，因此保留原弓已有数据，例如附魔、自定义名称、已损耐久和可保留的数据组件。

### 4.3 为什么需要自定义配方

原版锻造菜单对三个输入槽默认各扣 1，普通 `smithing_transform` JSON 不能表达模板 ×3 和下界合金锭 ×12。当前实现由两部分组成：

- `HouyisBowSmithingRecipe` 检查槽位物品与堆叠数量；
- `SmithingMenuMixin` 在原版各扣 1 之前额外扣除模板 2 个和下界合金锭 11 个。

最终总消耗为 3 / 1 / 12。

### 4.4 配方网络同步

- JSON codec 与网络 codec 共用同一个配方单例。
- 该结构避免 1.0.1/1.0.2 中出现过的 `Can't encode ..., expected ...`。
- 配方同步修复从 1.0.3 开始生效。

## 5. JEI 配方显示

### 5.1 用户表现

安装 JEI 时，后羿之弓配方显示在 JEI 原版锻造分类中，不新增自定义分类。

展示内容：

- 下界合金升级锻造模板 ×3；
- 普通弓 ×1；
- 下界合金锭 ×12；
- 后羿之弓 ×1。

### 5.2 可选依赖边界

- JEI API 仅以 `compileOnly` 参与编译。
- 后羿之弓 JAR 不包含 `mezz.jei` 类。
- 没有在 `neoforge.mods.toml` 中把 JEI 声明为强制服务端依赖。
- 后羿之弓主入口不直接引用 JEI 插件类。
- 未安装 JEI 时，物品注册、实际锻造和射箭仍应正常。

### 5.3 实现方式

- `HouYisBowJeiPlugin` 使用 `@JeiPlugin`。
- 通过 `registerVanillaCategoryExtensions` 注册 `HouyisBowSmithingRecipe` 扩展。
- 扩展分别提供 template、base、addition 和 output 四个槽位内容。

## 6. 拉弓时间

### 6.1 默认时间

- 默认满蓄力：10 game ticks。
- 在 20 TPS 下约等于 500ms。
- 实际射击前会把自定义蓄力时间归一化到原版 20 tick 满弓强度，继续使用原版弓蓄力曲线。

### 6.2 快速装填

| 快速装填等级 | 满蓄力时间 |
| --- | --- |
| 无 | 10 tick |
| I | 8 tick |
| II | 6 tick |
| III | 4 tick |
| IV 及更高 | 2 tick |

- 每级按 2 tick 缩短。
- 最低始终为 2 tick。
- 快速装填 30 等超高等级不会继续低于 2 tick。
- 没有使用原版弩完整 25 tick 基数直接套用，以免在低等级时直接降到 0。

## 7. 箭数量与方向

### 7.1 默认箭数

- 无多重箭附魔时，每次至少射出 3 支箭。
- 3 支箭从同一位置附近生成，并沿同一准星方向飞行。
- 不使用随机散射，也不让箭先朝错误方向飞行再修正到准星。

### 7.2 多重箭

- 多重箭效果以基础 3 支继续增加。
- 单次最多 12 支。
- 多重箭 20 等超高等级也受 12 支上限约束。
- 所有同次箭仍保持同一方向。
- 额外箭的弹药消耗和拾取行为继续走原版 projectile weapon 逻辑。

### 7.3 与旧数据包的关系

旧的 `LS-Multishot-Durability-Fix-1.21.1.zip` 所承担的多箭表现与耐久规则已经整合到 Mod。确认 1.0.5 在实例中正常后，用户可以删除该数据包，避免两个实现同时存在。

## 8. 耐久消耗

耐久按实际射出的箭数计算：

```text
耐久消耗 = ceil(箭数 / 3)
```

| 实际箭数 | 消耗耐久 |
| --- | --- |
| 1～3 | 1 |
| 4～6 | 2 |
| 7～9 | 3 |
| 10～12 | 4 |

正常机制下基础至少射出 3 支。表格保留 1～2 支区间是为了说明计算函数的完整边界。

- 一次射击只进行一次 `hurtAndBreak` 调用，并传入计算后的耐久点数。
- 耐久附魔仍会对这些耐久点使用原版减免机制。
- 不按每一支箭独立扣 1，因此 12 支箭不会扣 12 点。

## 9. 箭速与高速同步

### 9.1 箭速

- 原版满蓄力弓箭速度参数为 3.0。
- 后羿弓使用 3.5 倍原版速度。
- 满蓄力速度参数为 10.5。
- 未满蓄力时仍按原版蓄力曲线降低速度。

### 9.2 为什么需要自定义同步

原版实体生成/运动网络包会限制过高的速度分量。只在服务器把箭设为 10.5，会导致客户端最初看到的箭方向或速度不正确。

当前实现：

- 服务端生成 `HouyisArrowEntity`；
- 使用自定义 `ArrowStatePayload` 发送箭的初始位置和完整速度；
- 客户端在实体已存在时立即应用；
- 如果载荷先于实体到达，暂存最多 20 tick，并在客户端 tick 或实体加入时重试；
- 玩家开始追踪箭实体时，服务端再次发送状态。

### 9.3 协议版本

- 当前网络载荷协议：`0.0.1`。
- 这是协议兼容编号，不是 Mod 发布版本。
- 只有载荷结构发生不兼容变化时才需要调整。

## 10. 箭模型、伤害与击退

### 10.1 模型

- 使用原版光灵箭纹理和渲染外观。
- 实体不是原版 `SpectralArrow` 的发光效果实现。
- 命中目标后不会添加发光 Buff。

### 10.2 伤害

- 伤害走原版 `AbstractArrow` 机制。
- 拉弓强度影响继续使用原版弓曲线。
- 箭速度会参与原版伤害计算，因此高速箭的实际伤害会随速度提高。
- 力量附魔继续通过原版 fired weapon 数据参与命中判定。
- 没有额外自定义固定伤害或无视护甲机制。

### 10.3 击退

- 后羿弓自带等同原版 Punch IV 的基础击退。
- 冲击附魔仍按原版机制从弓上读取并叠加。
- 没有修改目标的击退抗性或其它实体属性。

## 11. 附魔兼容

### 11.1 支持范围

后羿弓通过物品标签同时适配弓系、弩系和耐久类附魔：

- 力量；
- 冲击；
- 火矢；
- 无限；
- 耐久；
- 经验修补；
- 快速装填；
- 多重箭；
- 穿透。

### 11.2 原版机制保持

- 无限仍要求玩家至少持有一支符合条件的箭，行为与原版弓一致。
- 火矢由原版投射物生成附魔回调点燃箭。
- 穿透由原版 `AbstractArrow` 机制处理。
- 力量和冲击从箭保存的武器副本读取。
- 耐久使用原版 `EnchantmentHelper.processDurabilityChange`。
- 多重箭与穿透仍位于原版互斥集合，正常附魔台/铁砧不能同时添加。

### 11.3 高等级兼容

- 原版正常途径的快速装填上限仍是 III。
- 如果命令、数据包或其它 Mod 提供 IV～30 等级，本 Mod 的计算仍能处理。
- 快速装填最终受 2 tick 下限约束。
- 多重箭最终受 12 支上限约束。
- 本 Mod 不修改其它附魔的注册上限和原版互斥定义。

## 12. 火箭火花拖尾

- 箭飞行时在箭尾生成原版火箭火花粒子。
- 不播放火箭推进或爆炸声音。
- 粒子从箭射出后持续生成。
- 箭命中实体、插入方块或停止飞行后不再生成新拖尾。
- 已经生成的粒子按原版粒子寿命自然消失。
- 拖尾只影响视觉，不改变伤害或命中判定。

## 13. 满蓄力监守者声波

### 13.1 触发条件

- 只有达到当前快速装填等级对应的满蓄力时间时触发。
- 从射手眼睛附近沿准星方向生成一系列声波粒子。
- 粒子步长沿瞄准方向直线排列。
- 当前距离上界常量为 22；实际从距离 1 生成到 21，共 21 个声波步进位置。

### 13.2 声音

- 使用原版 `entity.warden.sonic_boom` 声音事件。
- 音源类别：玩家。
- 音量：0.35。
- 音调：1.0。
- 没有额外自定义声音文件。

### 13.3 颜色

四种颜色交替使用：

| 颜色角色 | RGB 浮点值 | 视觉用途 |
| --- | --- | --- |
| 浅金 | `1.00, 0.96, 0.62` | 最亮高光 |
| 金色 | `0.99, 0.66, 0.04` | 接近 `#FDA901` 的主色 |
| 琥珀 | `0.97, 0.40, 0.01` | 暖橙过渡 |
| 深金 | `0.83, 0.47, 0.04` | 暗部层次 |

颜色按声波距离步进循环，因此不会整条光波只有一种颜色。

### 13.4 中性蒙版

- 1.0.5 之前直接使用原版青色声波贴图，金色乘色后会变绿。
- 1.0.5 使用 16 张白色 RGB / 原 alpha 蒙版。
- 四种颜色共用同一套蒙版。
- 保留原版 32×32 帧、轮廓、透明度、动画节奏和生命周期。
- 不使用自定义 shader，降低与 Iris、Veil、Sable 等渲染 Mod 的冲突面。

## 14. Northstar 首帧兼容

### 14.1 目的

Northstar 的客户端行星渲染会读取 SERVER 配置。在单人集成服务器尚未就绪或已经进入停止流程时，读取可能抛出 `Cannot get config value before config is loaded`。

### 14.2 当前保护

- 仅客户端 Mixin。
- 可选目标使用 `@Pseudo`，未安装 Northstar 时跳过。
- 精确注入 Northstar 六参数 `PlanetRenderer.render`。
- 仅在 `Minecraft.isLocalServer()` 为真时检查。
- 集成服务器为空、未 ready 或未 running 时取消该帧行星渲染。
- 下一帧服务器就绪后恢复。
- 远程多人不受影响。
- 注入发生在 Northstar 修改 PoseStack 或 GL 状态之前。

### 14.3 兼容边界

- 不修改 Northstar 文件。
- 不禁用 Northstar。
- 不依赖 Northstar 编译类。
- 不引用 Mixin 保留包中的普通辅助类。
- `require = 0`，未来 Northstar 目标签名变化时不会因为找不到注入点而阻止游戏启动，但保护可能失效，需要根据新日志更新。

## 15. 客户端与服务端职责

### 15.1 服务端负责

- 判断拉弓和射击；
- 选择及消耗箭；
- 计算箭数量和耐久；
- 生成箭实体；
- 处理伤害、击退、火矢和穿透；
- 发送完整高速箭状态；
- 生成满蓄力声波粒子与声音。

### 15.2 客户端负责

- 注册物品拉弓属性；
- 渲染后羿箭为光灵箭外观；
- 注册四种金色声波 provider；
- 接收并应用高速箭状态；
- 对先于实体到达的状态进行短期缓冲；
- 执行可选 JEI 展示和 Northstar 首帧保护。

## 16. 当前功能验收清单

### 16.1 基础物品

- [ ] 战斗页能看到后羿之弓。
- [ ] 创造搜索能找到后羿之弓。
- [ ] `/give @s houyis_bow:houyis_bow` 有效。
- [ ] 空闲与三段拉弓材质正确。

### 16.2 配方与 JEI

- [ ] JEI 能查看配方。
- [ ] JEI 显示 3 / 1 / 12。
- [ ] 材料不足时没有输出。
- [ ] 材料足够时可正常锻造。
- [ ] 取出成品后准确扣除 3 / 1 / 12。
- [ ] 原弓名称、耐久和附魔得到保留。

### 16.3 射击

- [ ] 无附魔默认射出 3 支。
- [ ] 多箭方向与准星一致。
- [ ] 3 支扣 1 耐久。
- [ ] 12 支扣 4 耐久。
- [ ] 箭速明显为原版约 3.5 倍。
- [ ] 高速箭客户端轨迹没有先偏移再修正。
- [ ] 箭显示为光灵箭，但目标不获得发光。
- [ ] Punch IV 等效击退正常。
- [ ] 火箭火花持续到命中/落地，不播放火箭声音。

### 16.4 附魔

- [ ] 快速装填 I～III 对应 8/6/4 tick。
- [ ] 快速装填 IV 及以上不低于 2 tick。
- [ ] 多重箭最多 12 支。
- [ ] 无限、火矢、力量、冲击、耐久、经验修补正常。
- [ ] 穿透按原版机制工作。
- [ ] 多重箭与穿透正常保持互斥。

### 16.5 声波与兼容

- [ ] 满蓄力播放低音量原版监守者音波声音。
- [ ] 声波为浅金、金、琥珀、深金交替，不再显示绿色。
- [ ] 进入新世界和已有世界不崩溃。
- [ ] 退出后重新进入世界不崩溃。
- [ ] Northstar 天空在服务器就绪后正常恢复。

## 17. 明确不属于当前功能

- 不给光灵箭命中目标添加发光 Buff。
- 不增加自定义固定伤害或真实伤害。
- 不修改普通原版弓。
- 不突破多重箭 12 支上限。
- 不突破快速装填 2 tick 下限。
- 不解除多重箭与穿透的原版互斥。
- 不播放火箭声音。
- 不增加自定义声波 shader。
- 不替换或修改其它 Mod。
- 不自动修改或迁移存档、地图、Xaero 地图和路径点。
