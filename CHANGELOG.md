# 更新日志

## 1.0.6 - 2026-08-18

- 同一次射击只让第一支实际生成的后羿箭产生火箭火花拖尾，其余箭保持完整伤害、弹道与命中效果，但不再重复发送拖尾粒子。
- 后羿箭插入方块后最多保留 100 tick（20 TPS 下约 5 秒），随后自动清理，降低连续射击造成的落地实体堆积。
- 未修改声波、箭数、箭速、耐久、伤害、击退、附魔、配方或其它 Mod。

## 1.0.5 - 2026-08-15

- 将后羿之弓加入创造模式“战斗”标签页，并同步进入创造搜索结果。
- 新增可选 JEI 19.39 锻造分类扩展，正确显示模板 ×3、普通弓 ×1、下界合金锭 ×12 与成品；JEI 不会被打包或变成服务端必需依赖。
- 将原版青色声波的 16 帧转换为共用中性蒙版，使浅金、金、琥珀、深金四种粒子颜色正确显示，不再被乘成绿色。
- 未修改任何其它 Mod、配置、存档、地图或路径点。

## 1.0.4 - 2026-08-11

- 修复进入世界后天空渲染触发的 `IllegalClassLoadError`：删除 Mixin 专用包中的普通 `NorthstarStartupGuard` 类，不再让注入后的 Northstar 类直接加载后羿之弓辅助类。
- 将本地服务器就绪判断直接内联到私有注入处理器；保留精确、可选的 Northstar 客户端保护，不增加新的兼容层。
- 未改动、禁用或替换任何其它实例 Mod、配置、存档、地图或路径点。

## 1.0.3 - 2026-08-11

- 修复进入世界时 `update_recipes` 同步失败：锻造配方的 JSON 与网络编码现在共用同一个配方实例，不再触发 `Can't encode ... expected ...` 断开。
- 修复 Northstar 可选客户端兼容 Mixin 的方法可见性错误，确保该保护可以正常应用。
- 未改动、禁用或替换任何其它实例 Mod、配置、存档、地图或路径点。

## 1.0.2 - 2026-08-11

- 新增仅客户端生效的 Northstar 首帧兼容保护：单人集成服务器尚未就绪或正在停止时，暂时跳过 Northstar 行星天空渲染，避免其过早读取未加载的 SERVER 配置。
- 不修改、禁用或替换 Northstar、Sable、Connector、ModernFix、Force Close Loading Screen 或其它实例 Mod。
- 统一 JAR、Manifest 与 Mod 列表版本为 1.0.2；网络载荷协议未变化，仍保持 0.0.1。

## 1.0.1 - 2026-08-11

首个独立物品版本。

- 恢复普通原版弓的数值、满蓄力时间和弹道；新增独立的“后羿之弓”。
- 后羿之弓耐久为 1000，基础蓄力 10 tick（约 500ms），每次基础射出 3 支同向箭。
- 加入快速装填 2 tick 下限、多重箭 12 支上限、每 3 支箭扣 1 耐久的规则。
- 后羿箭矢采用原版光灵箭外观，不附加发光；保留原版伤害、火矢、无限、耐久、穿透等机制。
- 加入 3.5 倍箭速、高速箭同步、Punch IV 等效基础击退与无声火箭火花拖尾。
- 满蓄力效果替换为低音量监守者音波声音与四种金色交替的监守者光波。
- 加入锻造台配方：3 个下界合金升级模板、1 把普通弓、12 个下界合金锭。
- 加入已确认的 64×64 空闲 / 三段拉弓材质与原版弓拉弓动画模型。
