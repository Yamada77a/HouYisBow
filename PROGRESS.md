# 后羿之弓开发路程

> 本文只记录项目为什么改变、每个版本新增/修改/删除了什么，以及各版本的验证结论。
>
> - 构建、测试、上传、远端切换与回滚记录：[PROGRESS_OPERATIONS.md](PROGRESS_OPERATIONS.md)
> - 当前全部功能及机制说明：[PROGRESS_FEATURES.md](PROGRESS_FEATURES.md)
> - 崩溃时间线、根因、失败尝试与续修规则：[BugGress.md](BugGress.md)

## 1. 当前开发状态

| 项目 | 当前状态 |
| --- | --- |
| 当前源码版本 | `1.0.6` |
| 当前 Windows 活动版本 | `houyis-bow-1.0.6.jar` |
| Minecraft | `1.21.1` |
| NeoForge | `21.1.233` |
| Java | `21` |
| 本地构建 | 1.0.6 已通过 |
| 本地测试 | 6 项强制重跑通过，0 失败 |
| Windows 部署 | 已完成，远端哈希与本地一致 |
| 1.0.6 性能优化 | 已构建并部署，待用户重启实测 |

当前发布产物：

```text
build/libs/houyis-bow-1.0.6.jar
```

SHA-256：

```text
00b42f69ffe755fb57a895ffec917ba0613e885384eef9aea5645de46bd3b29a
```

## 2. 版本迭代规则

- 独立“后羿之弓”的正式版本从 `1.0.1` 开始。
- 每完成一次常规修复或小型更新，默认将末位版本号加一，例如 `1.0.4` → `1.0.5`。
- 用户没有禁止时，可以根据变更性质提高中版本号或大版本号：
  - 新增一组较大的兼容能力或功能体系，可以提高中版本号；
  - 存在破坏兼容、存档格式变化或整体机制重做时，可以提高大版本号。
- 网络载荷协议版本与发布版本分开管理。当前箭状态同步协议仍为 `0.0.1`，不能为了显示版本变化而盲目修改。
- 每个版本必须同步更新 `gradle.properties`、`README.md`、`CHANGELOG.md`、`docs/releases/<版本>.md` 和三份 `PROGRESS` 文档。
- 如涉及崩溃或错误，还必须同步更新 `BugGress.md`。

## 3. 1.0.1：独立物品基线

发布日期：2026-08-11。

### 3.1 开发目标

早期实现直接影响普通原版弓。用户要求把普通弓的数值和行为完整还原，后羿弓改为一个独立注册的新物品，并把已经讨论过的高速、多箭、音波与锻造机制只应用到新物品上。

### 3.2 新增内容

- 注册独立物品 `houyis_bow:houyis_bow`。
- 新增 `HouyisBowItem`，继承原版弓的使用动作与弹药选择逻辑。
- 新增专用箭实体 `HouyisArrowEntity` 与客户端渲染器。
- 新增高速箭状态同步载荷，解决原版实体运动包对高速度分量的限制。
- 新增自定义锻造配方及 serializer。
- 新增锻造台额外扣料 Mixin，使配方可以真实消耗 3 个模板和 12 个下界合金锭。
- 新增 64×64 空闲材质和三段拉弓材质。
- 新增物品模型状态，使拉弓动画复用原版弓的 `pull` / `pulling` 属性。
- 新增飞行火箭火花拖尾和监守者音波效果。
- 新增弓、弩和耐久相关附魔标签适配。

### 3.3 机制调整

- 默认耐久设为 1000。
- 基础满蓄力时间设为 10 tick，20 TPS 时约 500ms。
- 默认一次射出 3 支箭。
- 所有同次射出的箭使用同一准星方向，移除随机方向偏移。
- 箭数上限设为 12。
- 每 3 支箭消耗 1 点耐久，使用向上取整规则。
- 满蓄力箭速设为同等原版弓箭速度的 3.5 倍。
- 自带等同原版 Punch IV 的基础击退。
- 箭模型使用原版光灵箭外观，但不附加发光 Buff。
- 命中、伤害、力量、火矢、无限、穿透等判定继续复用原版箭机制。
- 飞行时生成原版火箭火花粒子，不播放火箭声音；命中或落地后停止拖尾。
- 满蓄力射击播放原版监守者音波声音，音量降为 0.35。

### 3.4 附魔适配

- 快速装填按等级缩短拉弓时间：10、8、6、4、2 tick。
- 快速装填 IV 及更高等级仍保持 2 tick 下限。
- 多重箭从基础 3 支继续增加，但最多 12 支。
- 支持由其它 Mod、命令或数据包提供的高等级快速装填和多重箭。
- 支持力量、冲击、火矢、无限、耐久、经验修补和穿透。
- 多重箭与穿透仍沿用原版弩附魔互斥关系，没有额外加强。

### 3.5 删除和恢复

- 删除“所有普通弓都被强化”的全局行为。
- 恢复原版普通弓的耐久、拉弓时间、箭速、弹道和单箭行为。
- 后羿弓不再依赖旧的多重箭耐久数据包逻辑；确认 Mod 稳定后，旧数据包可以由用户删除。

### 3.6 已知问题

- 自定义配方的网络 codec 尚有对象一致性问题，之后在 1.0.3 暴露为 BG-002。
- 初始金色声波仍直接复用原版青色贴图，后续在 1.0.5 暴露为 BG-007。
- 物品已注册但未加入创造模式标签页，后续在 1.0.5 暴露为 BG-005。
- 自定义锻造配方没有 JEI 分类扩展，后续在 1.0.5 暴露为 BG-006。

## 4. 1.0.2：首次 Northstar 首帧兼容尝试

发布日期：2026-08-11。

### 4.1 问题背景

用户报告创建或进入世界时崩溃。末端堆栈显示 Northstar 在客户端天空渲染中读取尚未加载的 SERVER 配置。完整实例同时存在会提前关闭加载屏的 Mod，因此客户端可能在集成服务器就绪前开始渲染天空。

### 4.2 新增内容

- 新增仅客户端生效的可选 Northstar `PlanetRenderer` Mixin。
- 当本地集成服务器不存在、尚未 ready 或正在停止时，临时取消当帧 Northstar 行星渲染。
- 远程多人世界不走该判断。
- 不修改、禁用或替换 Northstar、Connector、Sable、ModernFix、Force Close Loading Screen 或其它实例 Mod。

### 4.3 版本与协议

- 发布版本统一调整为 `1.0.2`。
- JAR 文件名、Mod 元数据和 Manifest 使用同一发布版本。
- 箭状态同步协议没有结构变化，继续保持 `0.0.1`。

### 4.4 本版本失败点

- Mixin 类中存在一个非私有静态辅助方法。
- SpongePowered Mixin 0.8.7 不允许该结构，导致 Mixin 应用失败，即 BG-003。
- 因此 1.0.2 虽然本地能够构建，但新增保护在完整实例中没有真正生效。
- 此经历确定了一条长期规则：普通 JUnit 和成功打包不能替代真实 Mixin 转换验证。

## 5. 1.0.3：修复配方同步与第一次 Mixin 结构错误

发布日期：2026-08-11。

### 5.1 日志联查结论

完整日志表明，“加载到 100% 后保存世界”并不是存档损坏。首个异常是服务器向客户端发送 `update_recipes` 时无法编码 `HouyisBowSmithingRecipe`，导致玩家断开；之后集成服务器才进入保存世界和卸载配置流程。Create、BetterStats 和 Northstar 的配置异常出现在断开之后，属于下游连锁。

### 5.2 配方修复

- 为 `HouyisBowSmithingRecipe` 建立唯一 `INSTANCE`。
- JSON `MapCodec` 和网络 `StreamCodec` 共用同一实例。
- 删除“JSON 解码产生新对象、网络编码要求另一个固定对象”的 identity mismatch。
- 保留自定义配方、3/1/12 材料检查和锻造台额外扣料逻辑。

### 5.3 Mixin 修复尝试

- 把不允许存在于 Mixin 类中的普通辅助判断移到 `NorthstarStartupGuard`。
- 注入处理器本身改为私有静态方法，修复 1.0.2 的非私有静态方法错误。
- 保留精确六参数 `PlanetRenderer.render` 目标、客户端范围、`@Pseudo` 和 `require = 0`。

### 5.4 删除和排除

- 没有给 Create 或 BetterStats 增加兼容补丁，因为日志证明它们的配置读取异常发生在后羿弓配方断开之后。
- 没有修改其它 Mod，也没有删除用户要求保留的加载屏或渲染 Mod。
- 曾尝试添加普通 JUnit 配方 codec 测试，但该测试需要完整 NeoForge 注册表与 Mod 加载环境；为了避免伪造不真实的启动环境，该测试被移除，改用构建、字节码和完整实例验证。

### 5.5 验证与新问题

- 本地构建成功，6 项测试通过。
- JAR SHA-256：`87719eadf99cd9636f1b24517054915015460540cb6a3fe1bbc899bb27fa1715`。
- 完整实例已经越过配方同步，证明 BG-002 修复有效。
- 但 `NorthstarStartupGuard` 被放在 Mixin 配置包 `dev.houyisbow.mixin.*` 下。Mixin 运行时禁止直接加载该包中的普通类，于是产生 `IllegalClassLoadError`，即 BG-004。

## 6. 1.0.4：移除 Mixin 外部 Guard 依赖

发布日期：2026-08-11。

### 6.1 根因确认

SpongePowered Mixin 的配置包及所有子包只能放 Mixin 类。1.0.3 的 `NorthstarStartupGuard` 虽然不是 Mixin，却位于该保留包中；注入后的 Northstar 类第一次调用它时，类加载器主动拒绝加载。

### 6.2 修改内容

- 删除 `NorthstarStartupGuard` 普通辅助类。
- 删除只测试该辅助类布尔逻辑的单元测试。
- 把本地服务器判断直接内联到私有注入处理器：
  - 必须是单人本地服务器；
  - 服务器不存在、未 ready 或未 running 时取消当帧行星渲染；
  - 服务器就绪后立即恢复原 Northstar 行为。
- 保留 `@Pseudo`、精确方法描述符、客户端 Mixin 列表和 `require = 0`。
- 不新增反射、Northstar 编译依赖、Mixin 插件或新的兼容抽象层。

### 6.3 为什么保留最小保护

重新按时间线审查后，最初 Northstar 配置异常很可能是错误配方导致断开、停服和配置卸载后的下游现象。但 Northstar 源码确实在客户端渲染器中直接读取 SERVER 配置，实例又保留提前关闭加载屏的 Mod。因此最终保留一个只依赖原版服务器状态、没有外部辅助类的最小保险，而不是继续扩大兼容层。

### 6.4 验证结果

- `./gradlew --no-daemon clean test jar` 成功。
- 5 项单元测试通过。
- 反编译确认注入处理器是私有静态方法，字节码中没有 Guard 引用。
- JAR SHA-256：`11172e5652f22d7e04a862e128f91d58d87a3a463296f4befd4bc5587a380395`。
- 用户在完整实例中成功进入世界，未再报告 BG-001～BG-004 的保存世界崩溃。
- 该运行反馈支持 1.0.4 的配方与 Mixin 修复有效。

### 6.5 新发现但未在本版处理

- 后羿弓无法在创造模式战斗页或搜索页找到。
- JEI 不显示能够正常使用的锻造配方。
- 监守者声波显示为青绿或深绿色，而不是金色。

这三项分别归档为 BG-005、BG-006、BG-007，并统一进入 1.0.5，避免连续制作三个小修包。

## 7. 1.0.5：创造栏、JEI 与金色声波修复

发布日期：2026-08-15。

### 7.1 BG-005：创造模式入口

证据是“实际锻造正常，但战斗页没有物品”。这已经证明物品注册表、配方 serializer 和结果物品都正常，问题仅是注册物品没有参加创造标签页构建事件。

修改内容：

- 在现有 Mod 事件总线上监听 `BuildCreativeModeTabContentsEvent`。
- 当标签页为 `CreativeModeTabs.COMBAT` 时加入 `ModItems.HOUYIS_BOW`。
- 复用原版战斗页，不创建只有一个物品的自定义标签页。
- 物品同时进入创造搜索聚合结果。

### 7.2 BG-006：JEI 锻造显示

自定义配方能够实际工作，但 JEI 19.39 默认只认识拥有分类扩展的锻造配方。后羿弓配方不是原版 `SmithingTransformRecipe` 或 `SmithingTrimRecipe`，因此被 JEI 过滤。

修改内容：

- 添加 JEI 官方 Maven 仓库。
- 以 `compileOnly` 引入 `jei-1.21.1-neoforge-api:19.39.0.372`。
- 新增可选 `HouYisBowJeiPlugin`。
- 通过 `registerVanillaCategoryExtensions` 为 `HouyisBowSmithingRecipe` 注册扩展。
- 在原版 JEI 锻造页面显示模板 ×3、普通弓 ×1、下界合金锭 ×12、后羿之弓 ×1。
- 没有把 JEI 类打进 Mod，也没有把 JEI 变成服务端必需依赖。

### 7.3 BG-007：金色声波

原版声波贴图的可见 RGB 为青色 `#2CE3EB`。粒子 `setColor` 会做乘色，而不是替换贴图颜色；青色乘以 `#FDA901` 一类金色后会得到绿色，因此仅调高红色参数不能解决。

修改内容：

- 从原版 16 帧声波保留几何、分辨率和 alpha。
- 新建 16 张白色 RGB / 原 alpha 的中性蒙版。
- 四种声波粒子 JSON 共用这 16 张蒙版，共 64 个资源引用。
- 保留浅金、金、琥珀、深金四组现有颜色。
- 保留原版 `SonicBoomParticle` 的动画、尺寸、生命周期和渲染方式。
- 不创建 64 张重复的四色贴图，也不增加自定义 shader。

### 7.4 被测试拦截的失败尝试

第一次转换直接在原版索引色 `BufferedImage` 上调用 `setRGB`。因为图像只能使用旧青色调色板，写入白色又被映射回 `#2CE3EB`。新增资源测试立即失败，证明该产物仍会显示绿色。

最终改为新建 `TYPE_INT_ARGB` 图像，逐像素复制原 alpha 并把 RGB 写为白色。最终核验：

- alpha 差异：0；
- 可见非白像素：0；
- 可见像素：1513。

### 7.5 本地验证

- `./gradlew --no-daemon clean test jar` 成功。
- 6 项测试通过，0 失败。
- JAR 包含 16 张中性声波蒙版。
- 四个粒子 JSON 共 64 个引用全部指向 `houyis_bow:sonic_boom_mask_*`。
- `minecraft:sonic_boom_*` 引用数量为 0。
- JAR 中 JEI 插件类存在，打包的 `mezz/jei` 类数量为 0。
- 战斗标签页监听器字节码包含 `CreativeModeTabs.COMBAT` 和 `event.accept(ModItems.HOUYIS_BOW)`。
- JAR 文件名、`neoforge.mods.toml` 和 Manifest 版本均为 `1.0.5`。
- `git diff --check` 通过。

### 7.6 当前验证边界

- 1.0.5 已部署到 Windows 目标实例。
- 部署过程和远端哈希已经验证。
- 尚待用户在完整实例验证战斗标签页、创造搜索、JEI 的 3/1/12 数量、实际锻造、交替金色声波及射箭/附魔回归。

## 8. 1.0.6：连续射击性能优化

发布日期：2026-08-18。

### 8.1 问题分析

后羿弓最低可在 2 tick 满蓄力，并且单次可生成 3～12 支高速箭。1.0.5 中每一支箭每 tick 最多分别发送 12 次火箭粒子，因此箭数和射速会同时放大服务端粒子包、客户端粒子渲染与落地箭实体数量。原版落地箭约 60 秒后才清理，连续射击时会进一步累积实体 tick 成本。

### 8.2 修改内容

- 同一次射击仅第一支实际创建成功的 `HouyisArrowEntity` 保留拖尾。
- 其余箭复用既有 `trailStopped` 状态关闭拖尾，不新增同步字段、网络包或配置。
- 第一支箭仍保持原有每 tick 最多 12 个火箭粒子的密度与持续规则。
- 后羿箭落地后把原版 1200 tick 清理时间缩短为 100 tick，即 20 TPS 下约 5 秒。
- 计时仅在箭插入方块后进行；飞行时间不计入 5 秒。

### 8.3 保持不变

- 默认 3 支、最高 12 支箭及完全相同的准星方向；
- 3.5 倍箭速、伤害、击退、耐久和全部附魔逻辑；
- 满蓄力金色声波的 21 个步进粒子与低音量原版声音；
- 光灵箭模型、火矢、穿透、无限、拾取和高速同步；
- 创造标签页、JEI、锻造配方、Northstar 保护及其它 Mod。

### 8.4 预期改善

- 拖尾负载不再随单次箭数从 3 支放大到 12 支，而是固定为一条。
- 2 tick 连续满蓄力时，按箭在玩家 32 格接收范围内停留 2～3 tick 估算，拖尾加声波约为每秒 450～570 个粒子包调用；具体数量取决于箭离开接收范围的速度。
- 落地箭最大累积窗口从约 60 秒降到约 5 秒，理论上减少约 12 倍。

### 8.5 验证状态

- 源码实现完成。
- `./gradlew --no-daemon --rerun-tasks test` 已真实执行，6 项通过、0 跳过、0 失败、0 错误。
- `./gradlew --no-daemon clean test jar` 构建成功。
- 字节码确认第二支及以后调用 `disableTrail()`，并在 `inGroundTime` 达到 100 时 `discard()`。
- JAR 文件名、`neoforge.mods.toml` 和 Manifest 均为 1.0.6。
- Windows 目标实例已删除 1.0.5 并上传 1.0.6；远端哈希与本地一致，唯一活动后羿弓 JAR 为 1.0.6。
- 完整实例性能验收待用户重启后完成。

## 9. 错误编号与版本对应

| 编号 | 问题 | 修复版本 | 当前结论 |
| --- | --- | --- | --- |
| BG-001 | Northstar 配置尚未加载时被天空渲染读取 | 1.0.2 起增加保护，1.0.4 完成稳定实现 | 1.0.4 已成功进入世界，无同类反馈 |
| BG-002 | 自定义锻造配方在 `update_recipes` 同步时编码失败 | 1.0.3 | 完整实例已越过配方同步 |
| BG-003 | Mixin 中存在非私有静态辅助方法 | 1.0.3 | 原错误已消失 |
| BG-004 | 普通 Guard 类位于 Mixin 保留包并被直接引用 | 1.0.4 | 完整实例已成功进入世界 |
| BG-005 | 物品没有加入创造模式标签页 | 1.0.5 | 已构建、已部署，待界面实测 |
| BG-006 | JEI 不处理自定义锻造配方 | 1.0.5 | 已构建、已部署，待 JEI 实测 |
| BG-007 | 青色声波贴图与金色乘色后变绿 | 1.0.5 | 已构建、已部署，待粒子实测 |
| BG-008 | 高频多箭射击时粒子包、粒子实例与落地箭实体叠加造成明显卡顿 | 1.0.6 | 已完成最小源码优化，待构建与实测 |

## 10. 下一版本继续开发前

1. 先读取本文确认当前版本与开发边界。
2. 读取 [PROGRESS_OPERATIONS.md](PROGRESS_OPERATIONS.md) 确认最新构建和部署状态。
3. 读取 [PROGRESS_FEATURES.md](PROGRESS_FEATURES.md) 确认功能现状，避免重复实现。
4. 如涉及崩溃、错误或兼容问题，必须先读取 [BugGress.md](BugGress.md) 中所有既有尝试。
5. 1.0.6 完成后，新版本默认从 `1.0.7` 开始，除非按变更性质提升中版本号或大版本号。
6. 不修改、禁用、降级或替换用户实例中的其它 Mod，除非用户明确改变边界。
7. 存档、地图、Xaero 路径点不在本项目修改范围内。
